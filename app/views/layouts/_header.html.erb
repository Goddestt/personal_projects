<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <% if user%>
      <a class="navbar-brand "> Welcome </a>
      <a class="navbar-brand text-info" href="/information"> <%= user.name %></a>
    <% end%>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/">Home</a>
        </li>
        <% if user.is_a? User%>
          <li class="nav-item">
            <a class="nav-link active" href="/books/get_self_borrow_list">Book Borrowed</a>
          </li>
        <% end %>
        <li class="nav-item">
            <% if user %>
              <%= button_to "Logout", logout_path, method: :delete, class: 'btn btn-outline-success' %>
            <% else %>
              <%= button_to "Log in", login_path, method: :get, class: 'btn btn-outline-success' %>
            <% end %>
        </li>
      </ul>
    </div>
  </div>
</nav>